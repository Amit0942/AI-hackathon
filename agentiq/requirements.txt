pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0          # parquet cache for bookings / ridership_actuals + artifacts
pydantic>=2.7,<3
PyYAML>=6.0
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
python-multipart>=0.0.9

# Notebook runtime (Phase 1 exploratory data analysis)
jupyterlab==4.3.4
ipykernel==6.29.5

# No .docx library is needed: agentiq.data.briefs reads word/document.xml out of the
# zip with the standard library, which is the only way to keep table cells (the briefs'
# header fields) in document order.

# dev / test
pytest>=8.2
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
