# City profile — Las Hackland (LH)
# Populated from cities.csv / zone_demographics.csv (Phase 1). Values below are
# the raw facts already visible in the source data; anything modelling-derived
# (e.g. calibrated price multipliers) is added once Phase 1/6 complete.

city_id: LH
city_name: "Las Hackland"
market_tier: premium
transit_density: dense
timezone: "America/New_York"

pricing:
  market_tier_multiplier: 1.0   # calibrated in Phase 6; 1.0 = neutral placeholder

taxonomy_overrides: {}          # per-city label overrides, if any emerge in Phase 3
